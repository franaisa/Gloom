#ifndef __Logic_IIdentifer_H__
#define __Logic_IIdentifier_H__

/*namespace Logic{

	template <typename T>
	class IIdentifier {
	public:
		IIdentifier(const T& id);
		virtual ~IIdentifier(void);

		virtual T getId();

	private:
		T _id;
	};

	template <typename T>
	IIdentifier<T>::IIdentifier(const T& id) {
		this->_id = id;
	}

	template <class T>
	T IIdentifier<T>::getId() {
		return _id;
	}

};*/

#endif